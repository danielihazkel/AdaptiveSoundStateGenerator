/** Session length bounds shared by the duration picker and wall-clock math. */
export const MIN_CUSTOM_MINUTES = 5;
export const MAX_CUSTOM_MINUTES = 180;
/** "End at" can span a night's sleep. */
export const MAX_END_AT_MINUTES = 12 * 60;
